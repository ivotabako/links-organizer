<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:behaviors="clr-namespace:LinksOrganizer.Behaviors"
             xmlns:resources="clr-namespace:LinksOrganizer.Resx" 
             x:Class="LinksOrganizer.Views.OptionsView">
    <ContentPage.Content>
        <StackLayout Grid.Column="0" Grid.ColumnSpan="2" VerticalOptions="FillAndExpand"
                    Grid.Row="1" Orientation="Horizontal"  HorizontalOptions="End" >
            <Label VerticalOptions="Center" Style="{StaticResource SwitchLabelStyle}" Text="{x:Static resources:AppResources.OrderLabel}" />
            <Switch 
                        IsToggled="{Binding IsOrderedByRank}">
                <Switch.Behaviors>
                    <behaviors:SwitchOrderBehavior/>
                </Switch.Behaviors>
            </Switch>

            <Label VerticalOptions="Center" Style="{StaticResource SwitchLabelStyle}" Text="{x:Static resources:AppResources.ThemeLabel}"/>
            <Switch 
                         IsToggled="{Binding Theme}">
                <Switch.Behaviors>
                    <behaviors:SwitchThemeBehavior/>
                </Switch.Behaviors>
            </Switch>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>